# Настройка HTTPS для multiminder.duckdns.org

## 🔒 Зачем нужен HTTPS?

HTTPS обеспечивает:
- **Шифрование данных** между браузером и сервером
- **Защиту от перехвата** паролей и личных данных
- **Повышение доверия** пользователей
- **Требование для современных веб-приложений**

## 📋 Предварительные требования

1. ✅ Домен `multiminder.duckdns.org` должен указывать на IP вашего сервера
2. ✅ Порт 80 должен быть открыт в firewall
3. ✅ Nginx должен быть установлен и работать
4. ✅ Приложение должно быть доступно по HTTP

## 🚀 Быстрая настройка

### Вариант 1: Использование готового скрипта (рекомендуется)

```bash
cd ~/inventory-app
git pull origin main
chmod +x setup-https.sh
sudo ./setup-https.sh
```

Скрипт автоматически:
- Установит certbot (если не установлен)
- Получит SSL сертификат от Let's Encrypt
- Настроит Nginx для HTTPS
- Настроит автоматическое обновление сертификата
- Обновит конфигурацию приложения

### Вариант 2: Использование существующего скрипта

```bash
cd ~/inventory-app
chmod +x setup-ssl.sh
./setup-ssl.sh your-email@example.com
```

## 📝 Ручная настройка

Если скрипты не работают, выполните шаги вручную:

### 1. Установка certbot

```bash
sudo apt update
sudo apt install -y certbot python3-certbot-nginx
```

### 2. Получение SSL сертификата

```bash
sudo certbot --nginx -d multiminder.duckdns.org
```

Certbot автоматически:
- Получит сертификат от Let's Encrypt
- Настроит Nginx для HTTPS
- Настроит редирект с HTTP на HTTPS

### 3. Проверка конфигурации

```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 4. Проверка работы

Откройте в браузере: `https://multiminder.duckdns.org`

Вы должны увидеть:
- 🔒 Зеленый замок в адресной строке
- Автоматический редирект с HTTP на HTTPS

## 🔄 Автоматическое обновление сертификата

Let's Encrypt сертификаты действительны 90 дней. Certbot автоматически настроит обновление через systemd timer.

Проверить статус:
```bash
sudo systemctl status certbot.timer
```

Обновить вручную (если нужно):
```bash
sudo certbot renew
```

## 🛠️ Обновление конфигурации приложения

После настройки HTTPS нужно обновить `env.js`:

```bash
cd ~/inventory-app
sed -i 's|http://multiminder.duckdns.org|https://multiminder.duckdns.org|g' public/env.js
./rebuild-docker.sh
```

## ⚠️ Решение проблем

### Проблема: "Domain not pointing to this server"

**Решение:**
1. Проверьте DNS запись для `multiminder.duckdns.org`
2. Убедитесь, что домен указывает на правильный IP
3. Подождите распространения DNS (может занять до 24 часов)

### Проблема: "Port 80 not accessible"

**Решение:**
```bash
# Проверьте firewall
sudo ufw status
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

### Проблема: "Too many requests"

**Решение:**
Let's Encrypt ограничивает количество запросов до 5 в неделю на домен. Подождите несколько дней или используйте staging сервер для тестирования:

```bash
sudo certbot --nginx -d multiminder.duckdns.org --staging
```

### Проблема: "Nginx configuration error"

**Решение:**
```bash
# Проверьте конфигурацию
sudo nginx -t

# Просмотрите логи
sudo tail -f /var/log/nginx/error.log
```

## 📊 Проверка SSL сертификата

Проверить информацию о сертификате:
```bash
sudo certbot certificates
```

Проверить срок действия:
```bash
echo | openssl s_client -servername multiminder.duckdns.org -connect multiminder.duckdns.org:443 2>/dev/null | openssl x509 -noout -dates
```

## 🔐 Дополнительная безопасность

После настройки HTTPS рекомендуется:

1. **Включить HSTS** (уже включено в конфигурации certbot)
2. **Настроить firewall** для блокировки HTTP (не рекомендуется, так как нужен для редиректа)
3. **Регулярно обновлять** сертификаты (автоматически)

## 📚 Полезные команды

```bash
# Проверить статус certbot
sudo systemctl status certbot.timer

# Обновить сертификат вручную
sudo certbot renew

# Просмотреть все сертификаты
sudo certbot certificates

# Удалить сертификат (если нужно)
sudo certbot delete --cert-name multiminder.duckdns.org

# Тестировать обновление (dry-run)
sudo certbot renew --dry-run
```

## ✅ Проверка после настройки

1. ✅ Откройте `https://multiminder.duckdns.org` в браузере
2. ✅ Проверьте, что HTTP автоматически перенаправляется на HTTPS
3. ✅ Убедитесь, что приложение работает корректно
4. ✅ Проверьте консоль браузера на наличие ошибок
5. ✅ Проверьте, что все API запросы идут через HTTPS

## 🎉 Готово!

После настройки HTTPS ваш сайт будет:
- ✅ Защищен шифрованием
- ✅ Соответствовать современным стандартам безопасности
- ✅ Вызывать доверие у пользователей
- ✅ Автоматически обновлять сертификаты

