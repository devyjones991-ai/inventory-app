# 🔧 Исправление ошибки регистрации

## Проблема
При регистрации возникает ошибка, потому что приложение пытается подключиться к удаленному Supabase вместо локального.

## Быстрое исправление на сервере

### Вариант 1: Быстрый фикс (рекомендуется)

```bash
cd ~/inventory-app
git pull origin main
chmod +x fix-env-js.sh
./fix-env-js.sh
```

Скрипт автоматически:
- ✅ Создаст правильный `env.js` с локальным Supabase
- ✅ Пересоберет Docker образ
- ✅ Перезапустит контейнер

### Вариант 2: Полная переустановка

```bash
cd ~/inventory-app
git pull origin main
chmod +x setup-docker-production.sh
./setup-docker-production.sh
```

## Что было исправлено

1. **env.js создается автоматически** с правильными значениями локального Supabase
2. **Используется домен** `multiminder.duckdns.org` через nginx proxy
3. **Dockerfile обновлен** для копирования env.js из public директории

## Проверка после исправления

1. Откройте сайт: http://multiminder.duckdns.org
2. Откройте консоль браузера (F12)
3. Проверьте, что нет ошибок подключения к Supabase
4. Попробуйте зарегистрироваться

## Если проблема осталась

1. Проверьте, что Supabase запущен:
```bash
supabase status
```

2. Проверьте env.js в контейнере:
```bash
docker exec inventory-app-frontend cat /usr/share/nginx/html/env.js
```

3. Проверьте логи контейнера:
```bash
docker logs inventory-app-frontend
```

4. Перезапустите контейнер:
```bash
docker compose -f docker-compose.prod.yml restart app
```

